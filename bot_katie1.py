def choice (round_score, my score, opponent score):
       if my_score + round_score <= 100:
            return True
        else:
            return False
        else:
        return opponent_score > my_scores + opponent_score or my_score < 20
